# Eigenvector

A nonzero state that an operator maps to a scalar multiple of itself that is used to identify the state left unchanged in direction by that operator.

1\. If $A|\psi\rangle=a|\psi\rangle$ with $|\psi\rangle\neq 0$, then $|\psi\rangle$ is an eigenvector of $A$ belonging to $a$. This principle is used to name the post-measurement state when $a$ is obtained.

The eigenvector equation is

$$
A|\psi\rangle = a|\psi\rangle
$$

where

- $A$ is an operator.
- $|\psi\rangle$ is the eigenvector.
- $a$ is the corresponding eigenvalue.

2\. Eigenvectors of a Hermitian operator belonging to different eigenvalues are orthogonal. This principle is used to expand a general state in an orthonormal eigenbasis.

3\. After a measurement that yields $a$, the system is in the corresponding eigenvector. This principle is used to update the state.

Note: These principles are the definition of an eigenvector, orthogonality for Hermitian operators, and the post-measurement eigenstate. Also called an eigenket when the state is written as a ket. Also called an eigenfunction when the state is a wavefunction.

## References

1. Sakurai, J. J., & Napolitano, J. *Modern Quantum Mechanics*. Cambridge University Press, 2021. — eigenkets of observables.
2. Hall, B. C. *Quantum Theory for Mathematicians*. Springer, 2013. — eigenvectors of self-adjoint operators.
